# Dependencies
node_modules/

# Build
dist/
dist-ssr/

# Environment
.env
.env.local
*.local

# IDE
.vscode/*
!.vscode/extensions.json
.idea
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# OS
.DS_Store
Thumbs.db

# TypeScript
*.tsbuildinfo

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Local design scratch
mockups/

# Firebase CLI local cache
.firebase/

# React Native / Expo (in mobile/)
# Expo prebuild regenerates ios/ and android/ from app.json + plugins,
# so we don't track them — keeps the repo lean and the source of truth in JS config.
mobile/.expo/
mobile/ios/
mobile/android/
mobile/.env
mobile/.env.local
mobile/web-build/
mobile/expo-env.d.ts
